#include<iostream>
using namespace std;
int main(){
    int n,rem,ans=0,mul=1;
    cout<<"Enter the binary number: ";
    cin>>n;
      
      while(n>0){
        rem=n%10;
        n=n/10;
        ans=rem*mul+ans;
        mul*=2;

      }
      cout<<ans;
}
